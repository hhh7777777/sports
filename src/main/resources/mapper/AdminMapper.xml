<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongyuting.sports.mapper.AdminMapper">
    <!-- Admin 的 resultMap -->
    <resultMap id="adminResultMap" type="Admin">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="email" column="email"/>
        <result property="roleLevel" column="role_level"/>
        <result property="department" column="department"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="lastLoginTime" column="last_login_time"/>
    </resultMap>

    <!--  AdminLog 的 resultMap -->
    <resultMap id="adminLogResultMap" type="AdminLog">
        <id property="logId" column="log_id"/>
        <result property="adminId" column="admin_id"/>
        <result property="operation" column="operation"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="detail" column="detail"/>
        <result property="ipAddress" column="ip_address"/>
        <result property="operationTime" column="operation_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin (username, password, salt, email, role_level, department, status, created_at,last_login_time)
        VALUES (#{username}, #{password}, #{salt}, #{email}, #{roleLevel}, #{department}, #{status}, #{createdAt},#{lastLoginTime})
    </insert>

    <update id="updateLastLoginTime">
        UPDATE admin SET last_login_time = #{lastLoginTime} WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE admin SET password = #{password} WHERE id = #{id}
    </update>
    
    <update id="updatePasswordAndSalt">
        UPDATE admin SET password = #{password}, salt = #{salt} WHERE id = #{id}
    </update>

    <select id="findById" resultMap="adminResultMap">
        SELECT * FROM admin WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="adminResultMap">
        SELECT * FROM admin WHERE username = #{username} AND status = 1
    </select>
    
    <insert id="insertAdminLog" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO admin_log (admin_id, operation, target_type, target_id, detail, ip_address, operation_time)
        VALUES (#{adminId}, #{operation}, #{targetType}, #{targetId}, #{detail}, #{ipAddress}, #{operationTime})
    </insert>
    
    <select id="findAdminLogs" resultMap="adminLogResultMap">
        SELECT * FROM admin_log 
        <where>
            <if test='adminId != null'> AND admin_id = #{adminId}</if>
            <if test='operation != null'> AND operation LIKE CONCAT('%', #{operation}, '%')</if>
            <if test='startTime != null'> AND operation_time >= #{startTime}</if>
            <if test='endTime != null'> AND operation_time &lt;= #{endTime}</if>
        </where>
        ORDER BY operation_time DESC
    </select>
    
    <select id="findAdminLogsByTarget" resultMap="adminLogResultMap">
        SELECT * FROM admin_log WHERE target_type = #{targetType}
        <if test='targetId != null'> AND target_id = #{targetId}</if>
        ORDER BY operation_time DESC
    </select>
    
    <delete id="deleteAdminLogsBefore">
        DELETE FROM admin_log WHERE operation_time &lt; #{beforeTime}
    </delete>
    
    <select id="selectOperationCount" resultType="int">
        SELECT COUNT(*) FROM admin_log 
        <where>
            <if test='adminId != null'> AND admin_id = #{adminId}</if>
            <if test='startTime != null'> AND operation_time >= #{startTime}</if>
            <if test='endTime != null'> AND operation_time &lt;= #{endTime}</if>
        </where>
    </select>
</mapper>