<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongyuting.sports.mapper.UserMapper">
    <resultMap id="userResultMap" type="User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="salt" column="salt"/>
        <result property="wxOpenid" column="wx_openid"/>
        <result property="registerTime" column="register_time"/>
        <result property="userStatus" column="user_status"/>
        <result property="lastLoginTime" column="last_login_time"/>
    </resultMap>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password, salt, nickname, email, wx_openid, user_status, register_time)
        VALUES (#{username}, #{password}, #{salt}, #{nickname}, #{email}, #{wxOpenid}, #{userStatus}, #{registerTime})
    </insert>

    <update id="updateUser">
        UPDATE user SET
                        username = #{username},
                        password = #{password},
                        nickname = #{nickname},
                        salt = #{salt},
                        email = #{email},
                        wx_openid = #{wxOpenid},
                        user_status = #{userStatus}
        WHERE user_id = #{userId}
    </update>

    <update id="disableUser">
        UPDATE user SET user_status = 0 WHERE user_id = #{userId}
    </update>

    <select id="selectUserById" resultMap="userResultMap">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <select id="selectUserByUsername" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectUserByWxOpenid" resultMap="userResultMap">
        SELECT * FROM user WHERE wx_openid = #{openid}
    </select>

    <select id="selectAllUsers" resultMap="userResultMap">
        SELECT * FROM user WHERE user_status = 1
    </select>

    <update id="updateLastLoginTime">
        UPDATE user SET last_login_time = #{loginTime} WHERE user_id = #{userId}
    </update>

    <select id="countUsers" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

    <select id="selectUserCount" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

    <update id="updateUserPassword">
        UPDATE user SET password = #{password}, salt = #{salt} WHERE user_id = #{userId}
    </update>

    <select id="selectUserByEmail" resultMap="userResultMap">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM user WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM user WHERE email = #{email}
    </select>

</mapper>
